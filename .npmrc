engine-strict=true
# layercake declares peer typescript@^5, but the project is on TS 6.
# Both versions are ABI-compatible for layercake's runtime use, so accept
# the mismatch here and unblock `npm ci` (CI deploy + local installs).
legacy-peer-deps=true
